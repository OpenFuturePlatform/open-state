# MONGO
spring.data.mongodb.host=${MONGODB_HOST:localhost}
spring.data.mongodb.port=${MONGODB_PORT:27017}
spring.data.mongodb.database=${MONGODB_DATABASE:open_state}
spring.data.mongodb.username=${MONGODB_USERNAME:open_state}
spring.data.mongodb.password=${MONGODB_PASSWORD:open_state}
spring.data.mongodb.authentication-database=${MONGODB_AUTH_DB:admin}
spring.data.mongodb.auto-index-creation=false

# REDIS
spring.redis.host=${REDIS_HOST:localhost}
spring.redis.port=${REDIS_PORT:6379}
spring.redis.database=${REDIS_DATABASE:0}

# WATCHER
watcher.enabled=${WATCHER_ENABLED:true}
watcher.processor-fixed-delay=${PROCESSOR_FIXED_DELAY:360000}
watcher.block-number-updater-fixed-delay=${BLOCK_NUMBER_UPDATER_FIXED_DELAY:300000}
watcher.lock-time-out=${WATCHER_LOCK_TIMEOUT_IN_SECONDS:60}
watcher.ethereum.node-address=${ETHEREUM_NODE_ADDRESS}
